import React from "react";

/**
 * Компонент "Телепрограмма": выводит заголовок и телепрограмму относительно массива который приходит в пропсе links.
 * Каждая строка телепрограммы выводится комопнентом TvItem
 */

function TV({title, links, countToShow, showIcon}) {
    return 'Компонент "Телепрограмма"'
}

export default TV;
